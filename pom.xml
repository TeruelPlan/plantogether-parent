<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.9</version>
        <relativePath/>
    </parent>

    <groupId>com.plantogether</groupId>
    <artifactId>plantogether-parent</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>plantogether-parent</name>
    <description>Parent POM — centralizes dependency versions and build configuration for all PlanTogether modules</description>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!-- Internal library versions — update here to upgrade across all services -->
        <plantogether-common.version>1.7.0</plantogether-common.version>
        <plantogether-proto.version>1.0.0</plantogether-proto.version>

        <!-- Overrides Spring Boot's default Lombok version -->
        <lombok.version>1.18.38</lombok.version>

        <!-- Third-party versions not managed by Spring Boot BOM -->
        <grpc.version>1.73.0</grpc.version>
        <protobuf.version>4.29.3</protobuf.version>
        <protoc.version>4.29.3</protoc.version>
        <tomcat-annotations-api.version>6.0.53</tomcat-annotations-api.version>
        <aws-sdk.version>2.42.37</aws-sdk.version>
        <firebase-admin.version>9.3.0</firebase-admin.version>

        <!-- E2E test library versions -->
        <rest-assured.version>5.4.0</rest-assured.version>
        <assertj.version>3.25.3</assertj.version>
        <os-maven-plugin.version>1.7.1</os-maven-plugin.version>
        <protobuf-maven-plugin.version>0.6.1</protobuf-maven-plugin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Internal: common library (main + test-jar) -->
            <dependency>
                <groupId>com.plantogether</groupId>
                <artifactId>plantogether-common</artifactId>
                <version>${plantogether-common.version}</version>
            </dependency>
            <dependency>
                <groupId>com.plantogether</groupId>
                <artifactId>plantogether-common</artifactId>
                <version>${plantogether-common.version}</version>
                <classifier>tests</classifier>
                <scope>test</scope>
            </dependency>

            <!-- Internal: proto/gRPC stubs -->
            <dependency>
                <groupId>com.plantogether</groupId>
                <artifactId>plantogether-proto</artifactId>
                <version>${plantogether-proto.version}</version>
            </dependency>

            <!-- gRPC runtime -->
            <dependency>
                <groupId>io.grpc</groupId>
                <artifactId>grpc-netty-shaded</artifactId>
                <version>${grpc.version}</version>
            </dependency>
            <dependency>
                <groupId>io.grpc</groupId>
                <artifactId>grpc-stub</artifactId>
                <version>${grpc.version}</version>
            </dependency>
            <dependency>
                <groupId>io.grpc</groupId>
                <artifactId>grpc-protobuf</artifactId>
                <version>${grpc.version}</version>
            </dependency>
            <dependency>
                <groupId>io.grpc</groupId>
                <artifactId>grpc-testing</artifactId>
                <version>${grpc.version}</version>
            </dependency>

            <!-- Protobuf runtime -->
            <dependency>
                <groupId>com.google.protobuf</groupId>
                <artifactId>protobuf-java</artifactId>
                <version>${protobuf.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.tomcat</groupId>
                <artifactId>annotations-api</artifactId>
                <version>${tomcat-annotations-api.version}</version>
                <scope>provided</scope>
            </dependency>

            <!-- AWS SDK v2 — MinIO-compatible S3 client (file-service) -->
            <dependency>
                <groupId>software.amazon.awssdk</groupId>
                <artifactId>s3</artifactId>
                <version>${aws-sdk.version}</version>
            </dependency>

            <!-- Firebase Admin SDK — FCM push notifications (notification-service) -->
            <dependency>
                <groupId>com.google.firebase</groupId>
                <artifactId>firebase-admin</artifactId>
                <version>${firebase-admin.version}</version>
            </dependency>

            <!-- E2E test libraries -->
            <dependency>
                <groupId>io.rest-assured</groupId>
                <artifactId>rest-assured</artifactId>
                <version>${rest-assured.version}</version>
            </dependency>
            <dependency>
                <groupId>org.assertj</groupId>
                <artifactId>assertj-core</artifactId>
                <version>${assertj.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <distributionManagement>
        <repository>
            <id>github-parent</id>
            <name>GitHub Packages — parent</name>
            <url>https://maven.pkg.github.com/TeruelPlan/plantogether-parent</url>
        </repository>
    </distributionManagement>

    <!-- GitHub Packages repos for internal libraries — inherited by all modules -->
    <repositories>
        <repository>
            <id>github-parent</id>
            <name>GitHub Packages — parent</name>
            <url>https://maven.pkg.github.com/TeruelPlan/plantogether-parent</url>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>github-proto</id>
            <name>GitHub Packages — proto</name>
            <url>https://maven.pkg.github.com/TeruelPlan/plantogether-proto</url>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>github-common</id>
            <name>GitHub Packages — common</name>
            <url>https://maven.pkg.github.com/TeruelPlan/plantogether-common</url>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>

    <build>
        <pluginManagement>
            <plugins>
                <!-- Exclude Lombok from the repackaged fat JAR for all microservices -->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>
                <!-- Explicit annotation processor path so Lombok works in all modules -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <configuration>
                        <annotationProcessorPaths>
                            <path>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                                <version>${lombok.version}</version>
                            </path>
                        </annotationProcessorPaths>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
